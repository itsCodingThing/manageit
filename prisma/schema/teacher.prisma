model Teacher {
  id        Int      @id @default(autoincrement())
  school_id Int
  name      String
  email     String
  contact   String
  address   String?
  image     String?
  dob       DateTime @default(now())
  status    String   @default("active")
}
