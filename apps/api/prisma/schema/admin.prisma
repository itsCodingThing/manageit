model Admin {
  id          String   @id @default(uuid())
  name        String
  email       String   @unique
  phoneNumber String
  status      String   @default("pending")
  createdAt   DateTime @default(now())
}
